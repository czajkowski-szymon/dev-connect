spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devconnect
    username: postgres
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true